stages:
  - build
  #- test
  # https://hatchjs.com/gitlab-ci-artifacts-paths/
  - archive

variables:
  dummy_var: "$CI_PROJECT_DIR/dummy" 

build-linux:
  stage: build
  tags:
    - ubuntu-23
  except:
    - testing
    - stable
  script:
    - echo "Starting Linux build..."
    - mkdir gitlab-build-debug
    - cd gitlab-build-debug
    - /usr/bin/cmake
      -DCMAKE_BUILD_TYPE="Debug"
      -G"Ninja"
      -DCMAKE_C_COMPILER=/usr/bin/clang
      -DCMAKE_CXX_COMPILER=/usr/bin/clang++
      -DGITLAB_CI_PIPELINE_CREATED_AT="${CI_PIPELINE_CREATED_AT}"
      -DGITLAB_CI_PIPELINE_IID="${CI_PIPELINE_IID}"
      ..
    - /usr/bin/cmake --build . --target install -j4
    - echo "Linux build complete."
  artifacts:
    expose_as: avara3d-linux
    paths:
      - gitlab-build-debug/packaged/avara3d
    expire_in: 1 week

build-macos:
  stage: build
  tags:
    - macos-12.6
  except:
    - testing
    - stable
  script:
    - echo "Starting macOS build..."
    - mkdir gitlab-build-debug
    - cd gitlab-build-debug
    - /opt/local/bin/cmake
      -DCMAKE_BUILD_TYPE="Debug"
      -G"Ninja"
      -DGITLAB_CI_PIPELINE_CREATED_AT="${CI_PIPELINE_CREATED_AT}"
      -DGITLAB_CI_PIPELINE_IID="${CI_PIPELINE_IID}"
      ..
    - /opt/local/bin/cmake --build . --target install -j4
    - echo "macOS build complete."
  artifacts:
    expose_as: avara3d-macos
    paths:
      - gitlab-build-debug/packaged/avara3d
    expire_in: 1 week

build-windows:
  stage: build
  tags:
    - windows-10
  except:
    - testing
    - stable
  script:
    - echo "Starting Windows build..."
    - tar --version
    - mkdir gitlab-build-debug
    - cd gitlab-build-debug
    - cmake
      -DCMAKE_BUILD_TYPE="Debug"
      -T"ClangCL"
      -DGITLAB_CI_PIPELINE_CREATED_AT="${CI_PIPELINE_CREATED_AT}"
      -DGITLAB_CI_PIPELINE_IID="${CI_PIPELINE_IID}"
      ..
    - cmake --build . --target install -j8
    - echo "Windows build complete."
  artifacts:
    expose_as: avara3d-windows
    paths:
      - gitlab-build-debug/packaged/avara3d
    expire_in: 1 week

# test-linux:
#   stage: test
#   tags:
#       - debian-11
#   script:
#     - echo "Starting Linux test..."

# test-macos:
#   stage: test
#   tags:
#       - macos-12.6
#   script:
#     - echo "Starting macOS test..."

# test-windows:
#   stage: test
#   tags:
#       - windows-10
#   script:
#     - echo "Starting Windows test..."

.archive-debug-linux:
  stage: archive
  tags:
    - ubuntu-23
  environment: production
  only:
   - testing
   - stable
  dependencies:
    - build-linux
  script:
    - echo "Starting Linux debug archive..."
  artifacts:
    name: "$CI_JOB_NAME"
    paths:
      - gitlab-build-debug/packaged/avara3d
    expose_as: 'avara3d-linux'
    expire_in: 1 month

archive-debug-macos:
  stage: archive
  tags:
    - macos-12.6
  environment: production
  only:
   - testing
   - stable
  dependencies:
    - build-macos
  script:
    - echo "Starting macOS debug archive..."
  artifacts:
    name: "$CI_JOB_NAME"
    paths:
      - gitlab-build-debug/packaged/avara3d
    expose_as: 'avara3d-macos'
    expire_in: 1 month

archive-debug-windows:
  stage: archive
  tags:
    - windows-10
  environment: production
  only:
   - testing
   - stable
  dependencies:
    - build-windows
  script:
    - echo "Starting Windows debug archive..."
  artifacts:
    name: "$CI_JOB_NAME"
    paths:
      - gitlab-build-debug/packaged/avara3d
    expose_as: 'avara3d-windows'
    expire_in: 1 month

archive-release-linux:
  stage: archive
  only:
    - master
  # rules:
  #   - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"'
  environment: production
  tags:
    - debian-11
  script:
    - echo "Starting Linux release archive..."

archive-release-macos:
  stage: archive
  only:
    - master
  # rules:
  #   - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"'
  environment: production
  tags:
    - macos-12.6
  script:
    - echo "Starting macOS release archive..."

archive-release-windows:
  stage: archive
  only:
    - master
  # rules:
  #   - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"'
  environment: production
  tags:
    - windows-10
  script:
    - echo "Starting Windows release archive..."
